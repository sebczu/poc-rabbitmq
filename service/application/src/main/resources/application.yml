spring:
  application:
    name: poc-rabbitmq
  rabbitmq:
    port: 5672
    host: localhost
    username: admin
    password: test
#  listener:
#    acknowledge-mode: manual

management:
  endpoints:
    web:
      exposure:
        includ: "*"
